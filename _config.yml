title: CMOP
description: >- 
  Characterization of Constrained Multiobjective Optimization Problems 
url: "https://vodopijaaljosa.github.io" 
baseurl: "/cmop-web"

# Build settings
remote_theme: just-the-docs/just-the-docs
color_scheme: nice
# search_enabled: false # Use this to disable search

plugins:
  - jekyll-feed
  
kramdown:
    math-engine: katex

# Exclude from processing.
# The following items will not be processed, by default.
exclude:
  - .jekyll-cache/
  - Gemfile
  - Gemfile.lock
  - README.md
